{% extends 'base.html.twig' %}

{% block title %}Materiel{% endblock %}

{% block body %}
    <h1>Materiel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materiel.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ materiel.quantite }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('materiel_index') }}">back to list</a>
    <br>
    <a href="{{ path('materiel_edit', {'id': materiel.id}) }}">edit</a>

    {{ include('materiel/_delete_form.html.twig') }}
{% endblock %}
