<form method="post" action="{{ path('bon_intervention_delete', {'id': bon_intervention.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bon_intervention.id) }}">
    <button class="btn">Delete</button>
</form>
